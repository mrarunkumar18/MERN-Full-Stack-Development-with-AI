Arrays : Collection of multiple data in a Single Variable 

create Array : let arr = [1,2,3,4,5];

Arrays can hold different data types  

    Array is immutable  in the case of Strings 
    But Array is mutable in the case of Numbers 

    ** Loop over an Array **

        for(let i = 0; i<n; i++){
            
        } 

        for(let i = 0; i<arr.length; i++){
            
        }


        --> Array Methods : 

             1. push()
                pop()
                shift()
                unshift()
                splice()
                slice()
                concat()
                join()
                reverse()
                map()
                filter()
                reduce()
                forEach()
                find()
                findIndex()
                sort()

            Concat() : Combines two or more arrays. 

            unshift() : Adds one or more elements to the beginning of an array. 

            shift() : Removes the first element from the array

            slice() : returns a piece of an array 

            splice() : change original array : add, remove or replace elements

            

            

            
                